# 词缀脚本：技能-吸星大法
# 事件: OnRelease
# 说明: 技能释放请求通过服务端权威校验、资源和冷却提交前触发。
# 可用上下文: 可空当前单位、可空当前怪物、可空当前目标、当前技能、当前词缀。
# Tier 示例: 先写 Tier 当前Tier = 词缀.绑定Tier.获取(0)，再读取 当前Tier.变量.范围.当前值；变量类型必须与范围值类型一致。


矩形 吸取范围 = 获取矩形(当前单位.地图.所在位置, 当前词缀.Tier列表.技能_吸星大法.范围.当前值, 当前词缀.Tier列表.技能_吸星大法.范围.当前值)

有序集合<目标> 目标集合 = 当前单位.地图.查询目标(吸取范围)

遍历 元素 在 目标集合 中
    如果 元素.目标类型 == 列表<单位类型>.怪物
        坐标 玩家位置 = 当前单位.地图.所在位置
        矩形 随机范围 = 获取矩形(玩家位置, 1, 1)
        坐标 新位置 = 当前单位.地图.获取随机坐标(随机范围)
        元素.怪物.地图.所在位置 = 新位置
    结束
结束
