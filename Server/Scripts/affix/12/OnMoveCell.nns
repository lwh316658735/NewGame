# 词缀脚本：技能_鬼影步
# 事件: OnMoveCell
# 说明: 服务端接受一次走路或跑步并更新权威逻辑格后触发；跑步跨两格仍只触发一次。
# 可用上下文: 可空当前单位、可空当前怪物、当前地图、起点、终点、当前词缀。
# Tier 示例: 先写 Tier 当前Tier = 词缀.绑定Tier.获取(0)，再读取 当前Tier.变量.隐身几率.当前值；变量类型必须与范围值类型一致。
引用 "system/global/g_player_defaults.nns"
如果 移动方式 == 列表<移动方式>.跑步
    小数 触发概率 = 当前词缀.Tier列表.技能_鬼影步.隐身几率.当前值
    布尔 j = 随机概率(触发概率)
    如果 隐身 == 假 且 j
        隐身 = 真
        当前单位.隐身 = 列表<隐身状态>.高级隐身
        隐身任务.启动({
            睡眠(1000)
            当前单位.隐身 = 列表<隐身状态>.无
            隐身 = 假
        })
    结束
结束



