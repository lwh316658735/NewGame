# 词缀脚本：词缀偷取
# 事件: OnItemEquipmentChanged
# 说明: 服务端在词缀事件触发时执行本脚本。
# 可用上下文: 角色、当前物品、当前槽位、穿戴状态、当前词缀。

# 当前词缀没有可读取的 Tier 固化变量。

引用 "system/global/g_player_defaults.nns"

如果 穿戴状态 == 列表<穿戴状态>.穿戴
    猎首标记 = 真
否则
    猎首标记 = 假
结束
