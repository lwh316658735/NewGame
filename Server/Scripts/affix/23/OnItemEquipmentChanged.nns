# 词缀脚本：武器基底词缀
# 事件: OnItemEquipmentChanged
# 说明: 服务端在词缀事件触发时执行本脚本。
# 可用上下文: 角色、当前物品、当前槽位、穿戴状态、当前词缀。

# Tier 示例: 先写 Tier 当前Tier = 词缀.绑定Tier.获取(0)，再读取 当前Tier.变量.物理百分比.当前值；变量类型必须与范围值类型一致。
引用 "common/属性计算.nns"

如果 穿戴状态 == 列表<穿戴状态>.穿戴
    计算角色属性(角色,列表<伤害>.增加,0,当前词缀.Tier列表.通用武器基底词缀变量.物理百分比.当前值)
否则
    计算角色属性(角色,列表<伤害>.降低,0,当前词缀.Tier列表.通用武器基底词缀变量.物理百分比.当前值)
结束
