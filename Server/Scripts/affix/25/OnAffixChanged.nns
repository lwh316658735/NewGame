# 词缀脚本：智力转换
# 事件: OnAffixChanged
# 说明: 直接宿主获得、主动移除、出生、死亡或复活改变激活状态时触发；使用词缀状态区分拥有和失去，失去时当前词缀保留可读快照。
# 可用上下文: 当前词缀、词缀状态、服务器；物品事件提供当前物品，不提供角色、当前单位或当前怪物；单位和怪物事件沿用实体上下文。
# 分支示例: 如果 词缀状态 == 列表<词缀状态>.拥有
# Tier 示例: 先写 Tier 当前Tier = 词缀.绑定Tier.获取(0)，再读取 当前Tier.变量.魔法攻击力.当前值；变量类型必须与范围值类型一致。\
引用 "common/属性计算.nns"
如果 当前单位 != 空
    如果 词缀状态 == 列表<词缀状态>.拥有
        智力比值 = 当前词缀.Tier列表.属性转换.魔法攻击力.当前值
        智力转换 = 当前词缀.Tier列表.属性转换.智力.当前值
    否则
        智力比值 = 0
        智力转换 = 0
    结束
    更新智力转换魔法攻击力(当前单位)
结束