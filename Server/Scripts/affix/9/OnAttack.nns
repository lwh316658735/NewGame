# 词缀脚本：怪物-释放火球术
# 事件: OnAttack
# 说明: 角色每次发起服务端权威攻击周期时、动作和伤害结算前触发。
# 可用上下文: 可空当前单位、可空当前怪物、可空当前目标、可空当前技能、当前词缀。
# Tier 示例: 先写 Tier 当前Tier = 词缀.绑定Tier.获取(0)，再读取 当前Tier.变量.几率.当前值；变量类型必须与范围值类型一致。

整数 z = 1

小数 x = (小数)z *2.0

z = (整数)x