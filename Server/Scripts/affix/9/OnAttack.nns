# 词缀脚本：怪物-释放火球术
# 事件: OnAttack
# 说明: 角色每次发起服务端权威攻击周期时、动作和伤害结算前触发。
# 可用上下文: 可空当前单位、可空当前怪物、可空当前目标、可空当前技能、当前词缀。
# Tier 示例: 先写 Tier 当前Tier = 词缀.绑定Tier.获取(0)，再读取 当前Tier.变量.几率.当前值；变量类型必须与范围值类型一致。


布尔 jg = 随机概率(当前词缀.Tier列表.怪物_释放火球.几率.当前值)
如果 jg
    如果 当前单位 != 空
        遍历 元素 在 当前单位.技能 中
            如果 元素.名称 == "火球术"
                当前单位.释放技能(元素,列表<攻击目标>.最近, 假)
                跳出
            结束
        结束

    否则
        技能 火球术 = 当前怪物.获取临时技能(列表<技能>.火球术)
        如果 火球术 == 空
            当前怪物.添加临时技能(列表<技能>.火球术, 1)
            火球术 = 当前怪物.获取临时技能(列表<技能>.火球术)
        结束
        如果 火球术 != 空
            当前怪物.释放技能(火球术, 列表<攻击目标>.追踪目标, 假)
        结束
    结束
结束
