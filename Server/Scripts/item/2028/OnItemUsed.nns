# 消耗品专属使用脚本：物品编号 2028。
# 执行顺序：消耗品固定词缀 -> 本脚本 -> 全局使用物品脚本。
# 角色为使用者，当前物品为正在使用的消耗品，当前词缀为空。
# 目标物品为选中的背包物品只读快照；对自身使用时为空，不能修改其字段。
# 当前目标为鼠标指向的游戏实体，与目标物品不同。
# 空脚本正常结束，仍按消耗品配置扣除数量或次数并进入冷却。
如果 目标物品 != 空
    遍历 元素 在 当前物品.词缀 中
        目标物品.插槽.插槽词缀 << 元素.复制()
    结束
结束