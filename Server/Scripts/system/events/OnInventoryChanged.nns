# 每次成功背包操作汇总执行；真实登录将已有背包汇总为进入。
# 进入物品集合、离开物品集合仅含物品名称和数量。
# 集合及物品容器只读；脚本失败不撤销已提交的物品操作。

# 测试日志同时显示为当前区服系统公告。
遍历 条目 在 进入物品集合 中
    日志("[背包物品变化] 进入背包: {条目.物品实例.名称}({条目.物品实例.实例ID}), 数量: +{条目.数量}")
    如果 条目.物品实例.名称 == "{列表<物品>.小型护符}"
        遍历 元素 在 条目.物品实例.词缀 中
            如果 元素.名称 == "{列表<词缀>.护符之巨大的}"
                词缀 复制词缀 = 元素.复制()
                复制词缀.设置数据整数("id", 元素.实例ID)
                #日志("增加词缀实例ID:{元素.实例ID}")
                角色.词缀 << 复制词缀
            结束
        结束
    结束
结束

遍历 条目 在 离开物品集合 中
    #日志("[背包物品变化] 离开背包: {条目.物品实例.名称}({条目.物品实例.实例ID}), 数量: -{条目.数量}")
    如果 条目.物品实例.名称 == "{列表<物品>.小型护符}"
        遍历 元素 在 角色.词缀 中
            整数 变量名称 = 元素.获取数据整数("id")
            遍历 元素2 在 条目.物品实例.词缀 中
                如果 变量名称 == 元素2.实例ID
                    角色.词缀 >> 元素
                结束
            结束
        结束
    结束
结束
