# 系统事件脚本：OnPlayerLevelChanged
# 事件: OnPlayerLevelChanged
# 顺序: 对应词缀脚本全部成功提交后执行一次；没有可执行词缀时仍会执行。
# 当前词缀: 系统事件没有具体词缀，运行时值为 空。

# 事件能力: 与对应词缀事件一致。
# 失败: 只回滚本系统脚本的修改，保留已经成功提交的词缀结果。
引用 "common/属性计算.nns"

更新智力转换魔法攻击力(当前单位)