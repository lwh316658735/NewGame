# 说明: 处理固定界面“主界面 HUD”中按钮“透明点击区域按钮 27”点击后的操作。
# @参数 无。
# @返回 空 - 不返回业务结果。
# 函数: 按钮_link_c1e6ffe5_点击() -> 空
    # 设置 结果 = 角色.召唤("#{角色.名称}", 10000, 1000000)
    # 角色.发送消息("召唤{角色.名称}   召唤结果={结果}")
    设置 jg = 万分比乘法(100, 50000)
    设置 jg2 = 万分比除法(105, 50000)
    角色.发送消息("100* 5 = {jg}")
    角色.发送消息("100/ 5 = {jg2}")
    返回
